# BroadwayStream
simple stream a local file and render it with Broadway.js

you need:

- ffmpeg 
- node.js 
- a video file 


```
git submodule init
git submodule update

npm install

node index.js --help
```
